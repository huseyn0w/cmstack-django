# Core framework
Django==5.1.4
psycopg[binary]==3.2.3
django-environ==0.11.2

# Static files (prod serving) + Vite integration
whitenoise==6.8.2
django-vite==3.0.5

# Auth + social login (Google in this phase, more providers later)
django-allauth[socialaccount]==65.3.1

# Multilingual content: per-language translation tables for posts/pages/taxonomy
django-parler==2.4

# Image handling for avatars / media
Pillow==11.0.0

# Server-side HTML sanitization for rich-text content (Rust-backed, maintained)
nh3==0.2.20

# Spam protection: reCAPTCHA v3 on public forms (gracefully disabled without keys)
django-recaptcha==4.0.0

# Public REST API (read) + gated write API
djangorestframework==3.15.2

# WSGI server (used in prod, harmless in dev)
gunicorn==23.0.0
